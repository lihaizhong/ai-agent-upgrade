# 版本历史

## v7.0.8 (2026-04-08)

### 修正：整体审查发现的问题

#### 本次调整

**依赖缺失时给出明确错误**：
- 修复 `code/utils.py` 中 OpenAI / Anthropic SDK 导入失败后的错误变量
- 未安装依赖时，示例代码现在会返回明确的安装提示，而不是 `NameError`

**执行路径说明修正**：
- 更新 `SKILL.md` 中 Python 解释器回退示例
- 明确从 skill 目录执行时，应使用 `../../../.venv/bin/python` 或绝对路径

**课程目录完整性前置校验**：
- `scripts/engine.py` 现在会校验 `course_catalog.py` 中声明的课程文件和代码文件是否都存在
- 避免配置漂移时静默丢课，改为启动时直接失败

## v7.0.7 (2026-04-08)

### 优化：课程文案风格统一

#### 本次调整

**统一导航语气**：
- 为 06-16 课的“下一课”补充一句过渡说明
- 保持与 01-05 相同的课程串联风格

**统一结尾表达**：
- 移除课程结尾中的装饰性 emoji
- 保持整体文档语气更稳定、更一致

## v7.0.6 (2026-04-08)

### 优化：中段课程文档结构对齐

#### 本次调整

**统一 06-10 课的教学结构**：
- 补充核心优势、使用边界、常见误区和练习建议
- 让知识课程与工具课程的文档厚度更接近
- 保持现有课程编号、导航关系和脚本读取方式不变

## v7.0.5 (2026-04-08)

### 优化：后半程课程文档结构对齐

#### 本次调整

**统一 11-17 课的教学结构**：
- 为较短课程补充“何时使用 / 何时不该使用”
- 补充“常见误区”和“练习建议”
- 保持课程编号、文件名和脚本接口不变

**学习体验更一致**：
- 后半程课程不再只有概念和优势说明
- 与前半程课程更接近，便于连续学习
- 降低“后面课程越写越薄”的阅读落差

## v7.0.4 (2026-04-08)

### 优化：课程目录元数据收敛

#### 本次调整

**课程元数据单一来源**：
- 新增 `scripts/course_catalog.py`
- 将课程名、课程文件、代码文件、前置课程和摘要收敛到统一目录
- `scripts/engine.py` 不再内嵌 17 门课的大型元数据字典

**课程与代码绑定更稳定**：
- 课程加载从“按编号 glob 模糊匹配”改为“按目录清单精确读取”
- 代码讲解和课程内容读取共享同一份课程定义
- 降低课程重命名或新增时的漂移风险

**代码示例导入兼容性补强**：
- 为 `code/` 新增包入口 `__init__.py`
- 课程示例文件统一兼容包内导入和单文件运行两种方式

#### 文档与脚本更新

- 新增 `scripts/course_catalog.py`
- 更新 `scripts/engine.py`
- 更新 `code/*.py`
- 新增 `code/__init__.py`

## v7.0.3 (2026-04-08)

### 修正：模式选择强制优先使用 Question 工具

#### 本次调整

**模式选择不再只返回文本语义**：
- `scripts mode` 新增 `question` 字段
- 直接返回可供单选工具使用的标题、问题、选项和值
- 降低执行模型自行拼装交互时退化成文本菜单的概率

**模式选择规则补强**：
- 在 `SKILL.md` 中明确要求先运行 `scripts mode`
- 如果 `question` 工具可用，必须优先使用脚本返回的结构
- 明确禁止工具可用时输出 `1/2/3` 纯文本菜单

**回归约束补齐**：
- 更新 `evals/evals.json`
- 模式选择用例新增 `question / 选择模式` 断言

#### 文档与脚本更新

- 更新 `scripts/__main__.py`：为 `mode` 命令增加 `question` 结构输出
- 更新 `SKILL.md`：补充模式选择的强约束与标准示例
- 更新 `evals/evals.json`：增加 Question 工具相关断言

---

## v7.0.2 (2026-04-08)

### 修正：学习模式教学与选课体验

#### 本次调整

**先教学，再练习**：
- 学习模式必须先展示课程内容
- 课程讲完后再进入启发式提问、可选练习和学习导航
- 禁止把练习题当作课程开场

**六大类课程说明补齐**：
- 明确六大类课程名称与编号范围
- 为每个类别补充用途说明
- 为每门课程补充摘要，降低选课成本

**支持自由选课**：
- 课程不再要求严格按顺序学习
- 前置课程仅作为理解建议，不作为硬性门槛
- 新增 `scripts learn --category <类别>`，支持按类别单独列课

#### 文档与脚本更新

- 更新 `SKILL.md`：明确教学优先、六大类说明和自由选课规则
- 更新 `reference/learning-mode.md`：补充六大类映射、禁止直接出题、允许指定课程直达
- 更新 `scripts/engine.py`：增强课程类别元数据、课程摘要和自由选课提示
- 更新 `scripts/__main__.py`：新增 `learn --category` CLI 分支

---

## v7.0.1 (2026-04-07)

### 修正：技能说明与实现对齐

#### 本次调整

**固定 11 题，满分 100 分**：
- 选择题：固定 5 题 × 5 分 = 25 分
- 填空题：固定 3 题 × 10 分 = 30 分
- 大题：固定 3 题 × 15 分 = 45 分
- **总分严格等于 100 分**，不再浮动

**交互降级逻辑明确**：
- 优先使用单选工具
- 如果当前环境没有该工具，则退化为纯文本交互
- 选择题答案位置仍要求随机分布在 A/B/C/D 中

**系统学习选课补充约束**：
- 学习模式下的课程选择优先使用 `question` 工具
- 只有工具不可用时才退化为纯文本课程菜单
- 课程结束后的综合学习面板继续遵循同样规则

**结构与实现清理**：
- 精简 `SKILL.md`，移除非法 frontmatter 字段
- 新增 `agents/openai.yaml`
- 删除 `__pycache__` 产物
- 删除辅助性 `README.md`，保留核心课程、参考文档和脚本
- 移除 CLI 中暴露但未实现的考试生成参数

#### 文档更新

- 更新 `reference/exam-mode.md`：同步固定考试结构与降级交互策略
- 更新 `reference/learning-mode.md`：同步系统学习选课与综合面板的优先交互规则
- 更新 `reference/prompt-generation-mode.md`：补充 Script-First 约束
- 更新 `SKILL.md`：同步当前能力边界和资源用法
- 更新 `evals/evals.json`：同步版本号与考试结构描述

---

## v7.0.0 (2026-04-07)

### 重大更新：考试模式固定结构

#### 考试结构重构

**固定 11 题，满分 100 分**：
- 选择题：固定 5 题 × 5 分 = 25 分
- 填空题：固定 3 题 × 10 分 = 30 分
- 大题：固定 3 题 × 15 分 = 45 分
- **总分严格等于 100 分**，不再浮动

**选择题强制使用 Question 工具**：
- 所有选择题必须通过 Question 工具交互
- 答案位置随机分布（A/B/C/D）

**考前结构告知**：
- 开始前必须向学生展示分值构成
- 再生成题目

#### 文档更新

- 更新 `reference/exam-mode.md`：固定考试结构，明确 Question 工具要求
- 更新 `SKILL.md`：同步考试模式描述
- 更新 `evals/evals.json`：更新测试用例

---

## v6.0.0 (2026-03-28)

### 重大更新：代码示例集成

#### 新增代码实现

为 17 门课程全部添加 Python 代码实现示例：

- **code/utils.py**：通用工具函数（LLM 调用封装、答案提取、投票算法）
- **code/01_zero_shot.py** ~ **code/17_graph_prompt.py**：17 个课程代码文件

#### 代码覆盖

| 课程 | 技术 | 代码实现要点 |
|------|------|-------------|
| 01 | 零样本提示 | 基础调用、结构化输出 |
| 02 | 少样本提示 | 示例引导、格式控制 |
| 03 | 思维链 | 零样本/少样本触发、推理展示 |
| 04 | 自我一致性 | 多次采样 + 投票算法 |
| 05 | 思维树 | 树构建 + 评估 + 搜索策略 |
| 06 | 生成知识 | 两阶段生成流程 |
| 07 | RAG | 检索 + 生成完整流程 |
| 08 | 链式提示 | 多步骤串联 |
| 09 | ReAct | 推理循环 + 工具调用 |
| 10 | PAL | 代码生成 + 执行 |
| 11 | ART | 自动工具选择 |
| 12 | APE | 提示词生成 + 评估 |
| 13 | 主动提示 | 不确定性计算 |
| 14 | DSP | 刺激优化框架 |
| 15 | 自我反思 | 执行 → 评估 → 反思 → 改进 |
| 16 | 多模态 | 图文融合推理 |
| 17 | 图提示 | 图结构处理 |

#### 文档更新

- 更新 `SKILL.md`：版本 5.0.2 → 6.0.0，添加代码示例说明
- 新增 `code/README.md`：代码目录说明和使用指南

---

## v5.0.1 (2026-03-27)

### 优化：Skill 结构和性能

#### 性能优化

- **Description 精简**：从203字符缩减到120字符，提升触发精准度
- 更简洁的描述，保留核心功能和触发条件

#### 结构优化

- **添加目录导航**：为 learning-mode.md 和 exam-mode.md 添加完整目录
  - 690行和600行的大文档现在有关键章节导航
  - 符合 skill-creator 渐进式披露最佳实践
  
- **删除未使用文件**：移除 templates/learning-path.md（337行）
  - 该文件未被任何其他文件引用
  - 简化 skill 结构，减少维护成本

#### 代码清理

- 删除 exercises/ 目录（仅包含示例文件）
- 整体文件结构更加精简

---

## v5.0.0 (2026-03-27)

### 重大更新：学习模式启发式重构

#### 学习模式优化

**启发式学习流程**
- 从"填鸭式"改为"启发式"学习理念
- 课程结束后直接进入启发式提问环节（不问"有问题吗"）
- 提供4个简单提问角度：是什么/何时用/有何区别/怎么用
- 大白话表述，降低认知门槛

**综合学习面板**
- 同时展示：启发式提问 + 可选练习 + 学习导航
- 用户自主选择下一步，降低交互成本
- 不问"有问题吗"或"做题吗"

**练习题框架**
- 动态题型 + 强约束设计
- 6种题型根据课程特点选择（识别/诊断/设计/选择/对比/组合）
- 4项强制检查确保与课程核心概念强关联

#### 考试模式优化

**考试结构**
- 选择题5道，涵盖初级/中级/高级/专家四个难度
- 填空题3-4道，中级及以上难度
- 大题2-3道，考察提示词设计和多技术融合
- 共10-12题

**评价体系**
- 即时反馈：每题答完立即告知对错
- 严厉评价：考试结束提供严厉但建设性的评价
- 深度解析：选择题/填空题150字内精要解析，大题300字内深度剖析
- 结果保存：自动保存完整考试报告到 `exam-result/` 目录

#### 文档更新

- 重写 `reference/learning-mode.md`，启发式学习架构
- 更新 `reference/exam-mode.md`，重新设计考试结构
- 更新 `SKILL.md`，同步学习模式和考试模式描述
- 更新 `evals/evals.json`，更新测试用例

---

## v4.0.0 (2026-03-21)

### 重大更新：交互优化 + 提示词生成模式

#### 交互优化

- **单选交互**：将手动输入「学习/考试」改为 Question 工具单选交互
- 用户体验更直观便捷

#### 新增功能：提示词生成模式

通过结构化流程帮助用户生成高质量提示词：

**核心流程**：
1. **需求澄清**：引导式提问，澄清用户真实需求
2. **方案设计**：确定技术选型和提示词结构
3. **提示词生成**：根据方案生成初稿
4. **审查优化**：自动检查简洁性、明确性、完整性、可执行性
5. **用户确认**：确认提示词满足需求

**技术特点**：
- 头脑风暴引导清单，帮助深入理解需求
- 根据需求复杂度推荐合适的技术（零样本/少样本/思维链等）
- 生成后自动审查，保证提示词简单有效
- 支持多次迭代优化直到用户满意

#### 文档更新

- 新增 `reference/prompt-generation-mode.md` 详细说明
- 更新 SKILL.md，添加三大模式入口
- 更新 description，完善技能描述

---

## v3.1.1 (2026-03-19)

### 优化：考试结果命名规范化

- 考试结果保存至根目录 `exam-result/` 目录
- 文件命名格式：`<YYYY-MM-DD-HH-MM-SS>-<git-username>-prompt-learning-report.md`
- 自动获取 git username 作为考生标识
- 支持历史记录对比分析

---

## v3.1.0 (2026-03-19)

### 架构优化：内容分离，按需加载

- 精简 SKILL.md 至约 100 行
- 学习模式详细说明移至 `reference/learning-mode.md`
- 考试模式详细说明移至 `reference/exam-mode.md`
- 符合 skill-creator 最佳实践：核心配置精简，详细内容按需加载

---

## v3.0.0 (2026-03-19)

### 重大更新：学习模式 + 考试模式双轨制

#### 新增功能

**学习模式**
- 17门系统化课程，从入门到精通
- 精简优化的课程内容，更适合教学
- 互动问答机制，通俗易懂的解释
- 智能练习提醒，巩固学习效果

**考试模式**
- 四级难度：初级、中级、高级、专家
- 随机模式：仅从中级及以上选择
- 每场考试3-5题
- 即时讲解和评分（四维度评分）
- 考试总结报告，个性化改进建议
- 历史记录对比分析

#### 课程体系

- **基础课程**：零样本提示、少样本提示
- **推理课程**：思维链、自我一致性、思维树
- **知识课程**：生成知识提示、RAG、链式提示
- **工具课程**：ReAct、PAL、ART
- **优化课程**：APE、主动提示、DSP
- **前沿课程**：自我反思、多模态CoT、图提示

#### 文件结构优化

- 新增 `courses/` 目录存放课程内容
- 每门课程独立文件，按需加载
- 精简 SKILL.md 主文件，提升可维护性

---

## v2.6.0 (2026-03-17)

- 精简 skill 内容，提升可维护性
- 拆分参考文档为独立文件

## v2.0.0 (2026-03-15)

- 添加学习路径建议
- 优化提示词模式库
- 完善常见问题解答

## v1.0.0 (2026-03-10)

- 初始版本
- 基础提示词工程学习功能
- 练习题生成功能
